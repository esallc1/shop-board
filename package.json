{
  "name": "crisdata",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "CrisData static site + Vercel serverless functions (api/).",
  "scripts": {
    "test": "node --test 'api/*.test.js'"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  }
}
